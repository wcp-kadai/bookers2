if(<%= @render_flag %> === 0) {
  $('#book-<%= @book.id %>').html('<%= j(render 'books/book', book: @book) %>');
} else {
  $('.book-wrapper').html('<%= j(render 'books/content', book: @book) %>');
}
