<div class="message message-<%= chat_message.from_id %>">
  <div>
    <p class="grey"><%= chat_message.from.name %></p>
    <p class="alert alert-success" style="display: inline-block;"> <%= chat_message.content %> </p>
    <p class="grey "><%= chat_message.created_at.strftime('%Y/%m/%d %H:%M:%S') %></p>
  </div>
</div>
